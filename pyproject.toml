[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "artux-huginn"
version = "3.1.0"
description = "Cognitive module for the Artux AI system"
license = "MIT"

[tool.setuptools.packages.find]
include = ["huginn*"]
